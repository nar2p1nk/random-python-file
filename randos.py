from random1 import clock
from hashed import hashed,shuffleword
from pydub import AudioSegment
from pydub.playback import play
from reverse import reversal
from encrypted import encrypt,decrypt
ass = input('enter a number: ')

print(clock(int(ass)))
